# API Changelog

ZGI keeps the canonical project changelog at the repository root:

- `../CHANGELOG.md`

API-specific release notes should be added to the root changelog under the
same release version as the rest of the monorepo.
