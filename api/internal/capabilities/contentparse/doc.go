// Package contentparse provides a platform-level parsing capability boundary.
//
// The package is intentionally introduced as a foundation layer first:
// it compiles, has adapter seams, and can be adopted by modules later without
// changing current business behavior.
package contentparse
