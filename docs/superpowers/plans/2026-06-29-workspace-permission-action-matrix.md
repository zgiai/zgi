# Workspace Permission Action Matrix

Date: 2026-06-29
Branch: `px/permission-system-overhaul`
Status: live audit checklist

This matrix records the current workspace asset permission contract after the fine-grained role refactor. It is scoped to frontend route/action guards and backend workspace permission checks. Published runtime authorization remains a separate surface contract.

## Rules

- `workspace.*` permissions are governance or membership signals. They are not selectable ordinary member permissions.
- Organization owner/admin and workspace owner/admin keep governance authority. Ordinary members receive only asset action permissions.
- `prompt.*`, `content_parse.*`, and `dashboard.*` permissions are retired from member templates.
- Page-visible groups are only for navigation, list, empty state, and direct-page access. Buttons and mutations must use one action permission group.
- Mixed agent/workflow containers may use the combined visible group only for the shared list. Once an `agent_type` is known, detail layouts and sub-routes must branch to `AGENT_PERMISSION_ACTIONS.page` or `WORKFLOW_PERMISSION_ACTIONS.page`.
- Coarse legacy codes such as `agent.manage`, `knowledge_base.manage`, `database.manage`, `database.data_edit`, `database.ai_query`, `file.manage`, `file.upload_create`, and `file.move_create` may remain as compatibility inputs, but UI actions should consume the expanded action permission. These aggregate codes are not selectable in editable member/template permission lists.

## Matrix

| Module | Page Visible Group | Primary Action Permissions | Backend Contract | Current Strategy |
| --- | --- | --- | --- | --- |
| Agent assets | `AGENT_PERMISSION_ACTIONS.page` | `agent.create`, `agent.update`, `agent.delete`, `agent.move`, `agent.copy`, `agent.import`, `agent.export`, `agent.publish`, `agent.runtime_config.manage`, `agent.runtime_access.manage`, `agent.logs.view`, `agent.stats.view`, `agent.conversation.view`, `agent.conversation.manage` | Route-agent helpers choose agent permissions for agent runtime types. Backend API key management for AGENT runtime requires `agent.runtime_access.manage`; current detail API-key/docs UI remains workflow-only. | Keep fine-grained action checks. Keep `agent.view/manage` expansion only for legacy snapshots. |
| Workflow assets | `WORKFLOW_PERMISSION_ACTIONS.page` | `workflow.create`, `workflow.update`, `workflow.delete`, `workflow.move`, `workflow.copy`, `workflow.import`, `workflow.export`, `workflow.run.draft`, `workflow.run.stop`, `workflow.debug`, `workflow.publish`, `workflow.runtime_config.manage`, `workflow.runtime_access.manage`, `workflow.logs.view`, `workflow.stats.view`, `workflow.events.view` | Route-agent helpers choose workflow permissions for workflow runtime types. `workflow-runs` list/detail/node-execution APIs require `workflow.logs.view`; draft run, stop, and node debug use their own action permissions. | Keep workflow separate from agent. Editor save/canvas mutation uses `workflow.update`; draft run uses `workflow.run.draft`; stop uses `workflow.run.stop`; node debug uses `workflow.debug`; publish endpoint uses `workflow.publish`, while the frontend publish-current-draft action also requires `workflow.update` because it saves first; runtime access/webapp online-offline uses `workflow.runtime_access.manage`; run-history dropdown and logs link use `workflow.logs.view`. |
| Knowledge base | `KNOWLEDGE_BASE_PERMISSION_ACTIONS.page` | `knowledge_base.create`, `knowledge_base.update`, `knowledge_base.delete`, `knowledge_base.move`, `knowledge_base.folder_manage`, `knowledge_base.retrieval_test`, `knowledge_base.document.*`, `knowledge_base.segment.*`, `knowledge_base.index.manage`, `knowledge_base.graph.*` | Dataset access helpers use readable/action-specific sets. Folder management remains separate from document/segment actions. | Keep create/retrieval permissions in page-visible group so creators can enter the page. Direct documents, graph, hit-testing, and batch-testing pages must re-check their own action group. |
| Database | `DATABASE_PERMISSION_ACTIONS.page` | `database.create`, `database.update`, `database.delete`, `database.move`, `database.schema.*`, `database.record.*`, `database.import.*`, `database.ai_query.read`, `database.ai_query.write`, `database.table_prompt.*`, `database.guard_policy.manage`, `database.sql_audit.view`, `database.operation_logs.view` | Datasource list can use workspace visibility including create; detail uses existing-asset visibility. Direct current-table Excel import requires `database.import.execute`; AI-assisted table ingest requires `database.import.analyze` and persists through `database.record.create`. Import error-detail reads require `database.import.errors.view`; SQL audit reads require `database.sql_audit.view`; guard-policy reads/writes require `database.guard_policy.manage`. | Keep `database.data_edit` and `database.ai_query` as legacy compatibility, but prefer `record.*` and `ai_query.read/write` in UI/actions. Direct table, schema, import, BI search, operation-log, and table-prompt surfaces must re-check the matching action group. SQL audit and guard-policy permissions are retained as backend-capable controls; a full frontend management surface is not added in this pass. |
| Files | `FILE_PERMISSION_ACTIONS.page` | `file.upload`, `file.text.create`, `file.download`, `file.metadata.view`, `file.preview`, `file.update`, `file.delete`, `file.move`, `file.archive`, `file.folder.view`, `file.folder.manage`, `file.share.manage`, `file.favorite.manage`, `file.related.view` | File access helpers include create/upload aliases for page visibility and action-specific permissions for mutation. File detail uses readable permissions rather than download-only authorization. | Keep `file.upload_create` and `file.move_create` as compatibility aggregates; UI actions should use upload/text/move/folder actions directly. File detail is not opened by pure upload/folder-create entry permissions. |
| Workspace management | Membership and governance only | owner/admin authority, not member-template permissions | Backend treats workspace membership as workspace access; governance mutations require organization owner/admin or workspace owner/admin. | Do not add workspace governance permissions to ordinary member templates. UI management actions use `isWorkspaceManager()`. Workspace settings may show read-only basic information and self-leave for joined ordinary members, but ownership transfer/name edit/member permission changes stay manager-only. |
| Dashboard overview | none | none | Dashboard scopes derive visible workspaces from joined workspaces plus asset permission groups. | Everyone with organization context can open overview. Counts are narrowed by workspace membership and asset visibility. |
| Dashboard model/parser/channel configuration | organization owner/admin account capability | `can_manage_model_config` / organization admin capability, not workspace permission | Dashboard layout requires organization admin. LLM default-model writes, channel writes, and content-parse provider-settings read/write require organization owner/admin middleware. | Keep model/channel/parser configuration out of ordinary workspace member templates. Console overview may show readiness to all users, but configuration CTA is organization-admin only. |
| App center runnable apps | published runtime `app_center`, not workspace asset permission | grant subjects: organization, department, workspace, account | App-center list evaluates `app_center` surface candidates and supports no-workspace organization users. Explicit `app_center` surfaces are grant-scoped; legacy published apps without an `app_center` surface remain workspace-audience compatible. | Do not gate app-center on current workspace. Workspace grants mean audience, not builder permission. |
| Scheduled task | membership for list/detail, governance for mutation | create/update/draft/run/pause/resume/archive/delete use workspace manager authority | Backend uses `workspace.view` for reads and `workspace.manage` for mutations. | Frontend keeps the task page visible in workspace context, but management actions use `isWorkspaceManager()` rather than ordinary member permissions. |
| Prompt library/tools | workspace membership | no selectable prompt permission | Prompt APIs require current workspace `workspace.view`; prompt IDs are scoped to official or current workspace prompts. | Treat as workspace tool available to joined members. Do not reintroduce `prompt.*` member permissions. |
| Content parse playground | workspace membership | no selectable content-parse permission | User-facing playground requires current workspace `workspace.view`; internal HMAC routes stay separate. | Treat as workspace tool available to joined members. Do not reintroduce `content_parse.*` member permissions. |

## Change Log

- 2026-06-30: Organization owner/admin workspace context was re-smoked for accounts with no `workspace_members` row. Backend current-permissions and account-context switching already authorize organization admins through managed-workspace authority; the frontend workspace switcher now merges joined workspaces with managed workspaces for organization owner/admin users, so direct workspace pages show a selectable workspace state instead of a false no-workspace block, and selecting a managed workspace restores member, asset, file, database, and scheduled-task actions.
- 2026-06-30: Prompt/content-parse and workspace-governance contracts were re-audited. Prompt and content-parse user tools remain gated by concrete workspace membership (`workspace.view`) and frontend workspace access, without `prompt.*` or `content_parse.*` member permissions. Workspace governance permissions remain non-assignable; old ordinary-member snapshots containing `workspace.*` governance codes are ignored by `WorkspaceMemberAllowsPermission`, while owner/admin and organization owner/admin authority still bypass correctly.
- 2026-06-30: App-center runnable audit was refreshed after the `app_center`/`builtin_app` split. Agent app-center visibility is now recorded as the dedicated `app_center` runtime surface, while built-in workflows continue to use `builtin_app`; no-workspace organization users can see explicitly granted app-center apps through organization/account/department grants, and legacy apps without persisted `app_center` rows stay compatible only through workspace audience visibility.
- 2026-06-30: Dashboard configuration permissions were re-audited as organization governance rather than workspace asset permissions. The frontend dashboard shell remains behind the organization admin route guard, model/parser/channel configuration pages do not consume workspace permission helpers, LLM default-model writes and channel mutations stay behind organization owner/admin middleware, and content-parse provider-settings read/write remains organization owner/admin only.
- 2026-06-30: Dashboard recent-work agent/workflow scopes were split. The backend now carries separate `AgentWorkspaceIDs` and `WorkflowWorkspaceIDs`, filters `agents.agent_type` before returning recent asset and conversation items, emits workflow assets as `type=workflow`, and the frontend routes both agent and workflow recent assets through the canonical detail entry `/console/agents/:id` instead of letting workflow items fall through to database links.
- 2026-06-30: File text creation now has a dedicated sidebar entry and backend helper gated by `file.text.create`, while the upload entry and upload endpoint remain gated by `file.upload`. Mobile selector copy also distinguishes upload-capable users from text-create-only users so `file.text.create` no longer depends on upload visibility to reach its action.
- 2026-06-30: Database Excel import result details now respect the separate `database.import.errors.view` permission. Users with `database.import.execute` can complete imports and see the response summary, while the frontend only requests `/excel-import/jobs/:job_id/errors` when the import-error detail permission is also present.
- 2026-06-30: Workflow persisted run-event replay is now separated from runtime-log history in the frontend contract. The editor syncs `workflow.events.view` into the workflow store, and the console run/chat approval-resume streams will not call `/workflow-runs/:id/events` unless that permission is present, matching the backend event-stream handler.
- 2026-06-30: Scheduled-task backend route permissions are now locked by the frontend route-access guardrail. `GetTask`, `ListTasks`, and `ListTaskRuns` must keep `workspace.view`, while draft generation, create/update, manual run, delete, and state mutations must keep `workspace.manage`, matching the frontend `isWorkspaceManager()` mutation UI.
- 2026-06-29: Database table metadata reads were aligned across backend and frontend. Listing tables, reading table metadata, reading columns, and downloading table import templates now accept a table-metadata permission group covering schema, record, import, table-prompt, and AI-query capabilities; the database layout and overview only fetch/show table navigation when that same group is present. Operation-log-only users no longer trigger table-list requests or see table navigation.
- 2026-06-29: Database list create/edit gates were split. Opening the create dialog uses `database.create`, while opening the edit dialog now uses `database.update`, matching the card action and backend update endpoint instead of accidentally requiring create permission.
- 2026-06-29: Database list create and detail operation-log navigation gates now consume `DATABASE_PERMISSION_ACTIONS.create` and `DATABASE_PERMISSION_ACTIONS.operationLogsView` instead of raw permission literals, keeping frontend database UI actions bound to the shared action matrix.
- 2026-06-29: Database current-table import entries were split by backend contract. The simple Excel batch-import dialog now requires `database.import.execute`, while AI-assisted smart ingest requires both `database.import.analyze` and `database.record.create`; it no longer uses `database.ai_query.write`, which is reserved for database AI-query surfaces.
- 2026-06-29: Knowledge-base detail layout metadata fetching now waits for `KNOWLEDGE_BASE_VISIBLE_PERMISSION_CODES`. Direct `/console/dataset/:id` access no longer requests dataset metadata before the frontend has confirmed a knowledge-base visible permission; the root route still redirects to the first permission-compatible child page.
- 2026-06-29: Agent/workflow detail metadata fetching now waits for `AGENT_ASSET_VISIBLE_PERMISSION_CODES` in the shared detail layout, root redirect page, and sidebar. Users without any agent/workflow visible permission see the frontend access-denied state without first requesting the agent detail payload; users with a visible permission still resolve the runtime type and then hit the child-page-specific gates.
- 2026-06-29: File readable and browse permissions were separated. Existing-file detail, favorite filtering, and view authorization no longer treat `file.upload`, `file.text.create`, or folder-only access as file-view capability; list/folder/statistics browsing keeps a dedicated browse permission set so upload and folder workflows can still reach the file workspace surface. Frontend file detail and file-list detail actions now share the same existing-file readable/action gate.
- 2026-06-29: File detail download UI now consumes `FILE_PERMISSION_ACTIONS.download` instead of a raw `file.download` literal. `test-route-access` locks download/update actions to the file action matrix so future permission renames do not bypass the shared constants.
- 2026-06-29: Dataset card move actions were split by backend contract. Workspace transfer remains gated by `knowledge_base.move`, while moving a dataset into a knowledge-base folder now requires both `knowledge_base.move` and `knowledge_base.folder_manage`, matching the `dataset-folders/move-dataset` target-folder authorization.
- 2026-06-29: Dataset list create and folder-card edit/delete UI now consume `KNOWLEDGE_BASE_PERMISSION_ACTIONS.create` and `folderManage` instead of raw `knowledge_base.create` / `knowledge_base.folder_manage` literals. `test-route-access` now reads the dataset list and folder card sources so these knowledge-base actions stay bound to the shared action matrix.
- 2026-06-29: Agent card YAML export was narrowed to workflow runtime assets only. The only current export endpoint is workflow YAML export and it requires `workflow.export`; ordinary Agent cards no longer expose an `agent.export` action until a real Agent export endpoint exists.
- 2026-06-29: Agent list creation/import UI now separates blank-create permissions from workflow import/template permissions. Blank creation opens for `agent.create` or `workflow.create` and the create dialog hides runtime types the user cannot create; workflow YAML import and template creation are gated by `workflow.import` to match the backend import endpoint.
- 2026-06-29: Workspace asset-move service now resolves `agents.agent_type` before final permission checks for `agent` move requests. Shared asset-move UI may continue submitting type `agent`, but backend source/target workspace checks require `workflow.move` for workflow runtime agents and `agent.move` for ordinary agents.
- 2026-06-29: Agent/workflow create and legacy tenant move backend checks were tightened by runtime type. Creating workflow resources now requires `workflow.create` instead of `agent.create`; legacy `UpdateAgent` workspace changes require the matching `agent.move` or `workflow.move` permission in addition to update access, and the handler no longer performs a destination `agent.update` precheck that could reject workflow move users incorrectly.
- 2026-06-29: Added an idempotent migration data fix for already-migrated databases. Workspace role-template permission snapshots and non-owner workspace member snapshots are canonicalized again, expanding old aggregate inputs into fine action permissions while dropping retired `workspace.*`, `dashboard.*`, `prompt.*`, `content_parse.*`, and compatibility-only aggregate codes from stored editable snapshots.
- 2026-06-29: Workspace member list/detail/extension permission responses now return displayable canonical asset permissions. Old stored snapshots can still authorize through compatibility expansion, but API responses no longer expose retired `workspace.*`/`dashboard.*` codes or deprecated aggregate asset codes such as `agent.manage`, `database.ai_query`, and `file.upload_create` to frontend member-permission UI.
- 2026-06-29: Frontend scheduled-task management actions now use `isWorkspaceManager()` instead of generic workspace access, matching backend `workspace.manage` mutation checks without adding ordinary member `workspace.*` permissions.
- 2026-06-29: Agent/workflow detail layout now branches by loaded runtime type for page access. The shared list can still use the combined visible group, but detail shells no longer let AGENT visibility stand in for workflow visibility or vice versa.
- 2026-06-29: Backend API key management was aligned by runtime type: AGENT uses `agent.runtime_access.manage`, workflow/conversational workflow uses `workflow.runtime_access.manage`. The existing frontend API-key/docs detail route stays workflow-only per route-access contract.
- 2026-06-29: Workflow editor actions were split by exact permissions: save/canvas edit, run draft, stop run, node debug, publish, runtime access, and run history no longer share `workflow.update` or broad manage checks.
- 2026-06-29: Workflow canvas container drop overlays now consume the same read-only state as the main ReactFlow canvas. Permission-based read-only viewers can still inspect container nodes, but container-specific drag targets no longer render or create nested nodes when `workflow.update` is absent.
- 2026-06-29: Workflow node-creation entry points now share the same permission read-only boundary. Custom source handles, empty container add buttons, stale create-node modals, and delayed modal creation actions all require current `workflow.update` edit authority instead of relying only on history mode or on the panel entry being hidden.
- 2026-06-29: Workflow in-canvas node edits now use the same permission read-only boundary. Manual resize handles and note-node text/theme editing require current `workflow.update` edit authority, so permission read-only viewers cannot mutate dimensions or notes even outside the right-side node settings panel.
- 2026-06-29: Workflow shared write helpers now enforce the permission read-only boundary. `useNodeDataUpdate` and `useWorkflowOperations` block node-data updates, node creation, delete, paste, duplicate, and reset when the editor is in history mode or `workflow.update` is absent, so individual node managers are no longer the only protection layer.
- 2026-06-29: Workflow automatic node dimension sync now shares the same read-only boundary. DOM resize observation still runs for editable drafts, but permission read-only viewers no longer trigger React Flow node-internal re-measurement paths that can persist dimensions back into the graph.
- 2026-06-29: Workflow editor shell actions now keep local defensive read-only guards even when their parent container already hides them. Right-click menus, bottom toolbar add-note/undo/redo actions, keyboard shortcut attachment, and approval action-handle edge sync all require current `workflow.update` edit authority before mutating the graph.
- 2026-06-29: Dashboard model/parser/channel configuration was split from the open dashboard overview. The dashboard layout remains organization-admin gated, console overview uses `can_manage_model_config` for the configuration CTA, and content-parse provider-settings now mirrors LLM default-model/channel writes by requiring organization owner/admin middleware instead of relying on retired `content_parse.*` or `dashboard.*` workspace permissions.
- 2026-06-29: Workflow draft/config read now accepts the editor-reachability set (`workflow.view`, `workflow.create`, `workflow.import`, `workflow.update`, `workflow.run.draft`, `workflow.run.stop`, `workflow.debug`, `workflow.publish`, `workflow.runtime_config.manage`, `workflow.runtime_access.manage`) so direct editor entry does not fail before action-level guards load. Draft sync remains `workflow.update`; draft run/precheck remains `workflow.run.draft`; stop remains `workflow.run.stop`; node debug/diagnosis remains `workflow.debug`; publish remains `workflow.publish`.
- 2026-06-29: Workflow lifecycle normalization now distinguishes draft loading from draft mutation. Existing workflow drafts still load for permission-based read-only viewers, but empty-graph bootstrap, default-prompt normalization, and default model injection only run after `workflow.update` permission is resolved and granted.
- 2026-06-29: Workflow publish-current-draft UI now requires both `workflow.publish` and `workflow.update`. The backend publish endpoint remains keyed to `workflow.publish`, but the editor action persists the latest draft before publishing, so users without update permission no longer see a publish action that would publish stale or unsaved draft state.
- 2026-06-29: Agent runtime surface settings and WebApp online/offline management now use `agent.runtime_access.manage` end-to-end. Runtime configuration edit remains `agent.runtime_config.manage`, while publishing remains `agent.publish`.
- 2026-06-29: Agent runtime config read now accepts the same editor-reachability set used by the detail root (`agent.create`, `agent.import`, `agent.update`, `agent.publish`, `agent.runtime_config.manage`, `agent.runtime_access.manage`) so read-only runtime detail can load without granting edit authority. Workflow binding candidates, draft preview/chat, autosave, memory mutations, and runtime config updates still require `agent.runtime_config.manage`; published-version history/rollback stays behind `agent.publish`, and publish-only users publish the currently saved server draft without a forced autosave attempt.
- 2026-06-29: Agent runtime editor now propagates `agent.runtime_config.manage` into a read-only prompt/orchestration state. Users without runtime config permission can still inspect the runtime setup, but prompt/model/resource/memory/experience controls are disabled or no-op guarded.
- 2026-06-29: Agent API/logs audit was locked into frontend guardrails. The API key/docs detail route remains workflow-only and requires `workflow.runtime_access.manage`; the logs detail route branches by runtime type, gating workflow run logs with `workflow.logs.view` and AGENT runtime logs with `agent.logs.view` before issuing list/detail queries.
- 2026-06-29: AGENT runtime history compatibility APIs now share the same backend `agent.logs.view` contract as the dedicated runtime-log APIs. Dispatch routes for conversations, conversation detail, chat messages, and runtime logs resolve the agent workspace from the AGENT record and deny access before querying runtime history when the caller lacks `agent.logs.view`.
- 2026-06-29: Agent runtime knowledge binding UI now uses `KNOWLEDGE_BASE_READ_PERMISSION_CODES`, mirroring backend knowledge binding validation. Pure `knowledge_base.create` or `knowledge_base.retrieval_test` access no longer opens knowledge candidate/detail requests or mutation controls for agent binding.
- 2026-06-29: Workflow knowledge-retrieval node configuration now uses `KNOWLEDGE_BASE_READ_PERMISSION_CODES` for knowledge candidate loading and dataset selection. Users who can edit workflow nodes but lack knowledge-base readable permissions no longer trigger knowledge-base list requests from the node panel; recall setting controls also honor workflow read-only state so model defaults and sliders do not mutate read-only workflows.
- 2026-06-29: Agent runtime database binding UI now matches backend binding validation. Binding database tables requires both `database.ai_query.read` and `database.record.view`; writable table authorization still additionally requires record mutation permissions. Without the read-binding pair, the editor keeps existing bindings visible as unavailable/read-only context and does not load database/table candidates.
- 2026-06-29: Workflow call-database and SQL-generator database metadata UI now uses the same read-binding requirement as agent runtime database binding: `database.ai_query.read` plus `database.record.view`. Users who can edit workflow nodes but lack the database read-binding pair no longer open database candidate, table-list, or column-list requests from the picker, insert menus, expanded SQL editor, or SQL-generator table enrichment path.
- 2026-06-29: Frontend selectable permission lists now hide compatibility-only aggregate codes `database.data_edit`, `database.ai_query`, `file.upload_create`, and `file.move_create`. Permission normalization expands those old codes into precise action permissions before saving, so editing an old snapshot does not silently lose equivalent capabilities.
- 2026-06-29: Compatibility-only aggregate permissions are now input-only across member/template snapshots and frontend action groups. Backend canonical snapshots expand `database.data_edit`, `database.ai_query`, `file.upload_create`, and `file.move_create` without retaining the old codes; frontend database/file direct pages and buttons now use exact `record.*`, `ai_query.*`, `file.upload`, and folder/file action permissions.
- 2026-06-29: File upload backend authorization now checks `file.upload` instead of legacy `file.upload_create`. File/dashboard visible permission helpers request fine-grained file permissions while still allowing old `file.upload_create` or `file.move_create` grants through compatibility expansion.
- 2026-06-29: File related-resource links now route dataset references through the permission-aware dataset detail root instead of assuming document-page access.
- 2026-06-29: Data-library knowledge-base file-reference routes stopped using legacy `knowledge_base.manage`. Candidate listing and ref creation now use `knowledge_base.document.create`, ref listing uses `knowledge_base.document.view`, embedding/retry task actions use `knowledge_base.index.manage`, and removal uses `knowledge_base.document.delete`.
- 2026-06-29: Workflow batch-test frontend routing now matches backend action split. Case library requires `workflow.view`; batch/result direct pages require `workflow.logs.view`; creating and immediately executing a batch requires `workflow.view`, `workflow.logs.view`, `workflow.update`, and `workflow.debug`. Batch-test buttons now hide update-only actions behind `workflow.update`, generation/retest/start actions behind `workflow.debug`, and cancellation behind `workflow.run.stop`.
- 2026-06-29: Dataset detail root routing now chooses the first permission-compatible child page instead of always redirecting to documents. Document-capable users still land on documents; retrieval-test-only users land on hit-testing; graph-only users land on graph when graph flow is enabled; settings-capable users land on settings.
- 2026-06-29: Dataset settings access was aligned with the backend dataset-update contract. The settings route and sidebar entry now require `knowledge_base.update` only, and the direct settings page does not fetch dataset metadata before that permission is present. `knowledge_base.index.manage` and `knowledge_base.graph.manage` remain action/page permissions for indexing and graph-specific surfaces, but they no longer imply access to the dataset-level settings form.
- 2026-06-29: Dataset list cards now link to the permission-aware detail root instead of directly opening documents, so retrieval-test-only, graph-only, or settings-capable users land on the matching child page.
- 2026-06-29: Dataset creation now routes to the permission-aware detail root instead of assuming the documents child page is available. Hit-testing result document-detail links are hidden unless `knowledge_base.document.view` is present, matching the document direct-page gate.
- 2026-06-29: Agent/workflow detail root routing now chooses the first permission-compatible child page instead of jumping by runtime type alone. AGENT runtime users land on the runtime editor only with runtime config, publish, or runtime-access permission, otherwise published logs can be the fallback. Workflow users land on the editor only with an editor action, otherwise API key/docs, published logs, or batch-test pages are selected by their exact action permissions; the detail sidebar no longer shows the editor entry unconditionally.
- 2026-06-29: Agent/workflow direct editor pages now share the same editor-reachability boundary as the detail root. `/console/agents/:id/agent` only fetches agent metadata/runtime config after an AGENT editor-related permission is present, and `/console/agents/:id/workflow` only fetches metadata after a workflow editor-related permission is present. Workflow runtime-config managers are included in root/sidebar/direct-page editor reachability, while logs/API/batch-test-only users still fall back to their matching child pages instead of loading the editor.
- 2026-06-29: Agent/workflow API, logs, and batch-test direct child pages now gate agent metadata fetching by their page-specific permissions. Workflow API docs/keys require `workflow.runtime_access.manage`; runtime logs require the matching AGENT or workflow `*.logs.view`; batch-test case-library requires `workflow.view`; batch list/result pages require `workflow.logs.view`; new batch creation requires `workflow.view`, `workflow.logs.view`, `workflow.update`, and `workflow.debug`. `workflow.debug` no longer widens batch-test page visibility on the detail root or sidebar; it remains an execution/action permission inside pages that are already readable.
- 2026-06-29: Workspace member management was tightened to the fixed governance model. `workspace.member.view` no longer behaves like a general membership permission; backend member-list authorization now requires workspace owner/admin authority or organization owner/admin bypass, and the frontend members route/sidebar/overview links only fetch or enable member management for that manager state. Joined non-manager members still keep `workspace.view`-style workspace context and can use the settings page for basic info/self-leave.
- 2026-06-29: Workspace member lookup was split by use case. Organization-scope member management endpoints now require `workspace.member.view`, while workflow approval recipient pickers use new `member-options` endpoints authorized by `workspace.view` and returning only basic account/contact fields. This preserves builder workflows that need to pick approvers without exposing role, permission, or governance data to ordinary workspace members.
- 2026-06-29: Organization-scope workspace governance endpoints now have handler-level regression coverage for the intended boundary: member management detail stays behind `workspace.member.view`, business member-option detail stays behind `workspace.view` and omits role/permission fields, and workspace asset-count checks stay behind `workspace.member.manage`.
- 2026-06-29: Legacy `/workspaces/current/members...` handler routes were aligned with the same governance boundary. Current member detail now requires `workspace.member.view`; current invite and current delete check `workspace.member.manage` before account/member lookup or invitation side effects; current role updates now resolve the active workspace join instead of using the caller's first joined workspace.
- 2026-06-29: Legacy workspace member invite routes now explicitly reject direct `permissions` payloads instead of accepting or ignoring a second permission-entry path. Current dataset-operator member lookup now requires `workspace.view`, and current default email invite requires `workspace.member.manage` before workspace lookup or invitation side effects.
- 2026-06-29: Applying a workspace role template to members now delegates authorization to the per-target workspace member update checks instead of requiring organization owner/admin at the handler boundary. Workspace owners/admins can apply an available role template to members in workspaces they manage, while unauthorized targets remain failed results from the service layer.
- 2026-06-29: Frontend workspace member management now delays both the member roster and role-template list queries until fixed manager authority is known. Direct `/console/workspace/members` access by ordinary members renders the denied state without prefetching role-template permission snapshots.
- 2026-06-29: Workspace role-template read surfaces now use the same fixed manager authority on the backend. Permission-definition catalog, role-template list, and role-template detail require organization owner/admin authority or at least one managed workspace, so ordinary organization members cannot fetch template permission snapshots by direct API.
- 2026-06-29: Frontend member-assignment and runtime-audience dialogs now defer role-template, department, and workspace-scope catalog queries until the dialog is open. The primary publish settings chip summary keeps label lookup enabled for saved grants, while the secondary picker avoids closed-dialog organization-scope prefetches.
- 2026-06-30: Frontend workspace role-template selection now uses one shared predicate for add-member dialogs, member-permission dialogs, the organization permission-template page, and the organization workspace detail page. Fixed governance roles and legacy builtin roles stay classified in one place, preventing a looser owner-only filter from exposing non-selectable templates through a secondary management entry.
- 2026-06-30: Organization current-member lookup now has a dedicated visible-member contract. Organization owner/admin still receive the full organization member list, while ordinary organization members and workspace managers are narrowed to themselves, members in their own department subtree, and members of workspaces they can view. Runtime-audience and member picker flows keep working for non-admin builders without exposing unrelated organization members.
- 2026-06-29: Agent/workflow list cards now link to the permission-aware detail root instead of directly opening the editor. This keeps daily list navigation aligned with the same editor/API/log/batch-test fallback used by direct `/console/agents/:id` access.
- 2026-06-29: Agent/workflow detail-root editor reachability now also includes create/import/update-style asset entry permissions so custom templates with create/import access do not create or list assets that cannot be opened. Mutating controls inside the editor still use their exact action permissions.
- 2026-06-29: Prompt usage references now link through the permission-aware agent detail root and the root preserves query parameters such as `nodeId`, so prompt tooling does not bypass workflow detail permissions while still supporting node focus when the editor is the selected child page.
- 2026-06-29: File list direct access now matches sidebar visibility. `/console/files` requires `FILE_VISIBLE_PERMISSION_CODES` before rendering `FileManagementContent`, so a member without any file permissions sees the shared access-denied state instead of an empty file table.
- 2026-06-29: Workspace governance audit confirmed the intended owner/admin model is still the authority boundary. Frontend workspace member/settings and scheduled-task mutation actions use manager state (`organization owner/admin` or `workspace owner/admin`), while backend member/permission/task mutations require workspace governance permissions that are granted only through owner/admin authority. `workspace.view` remains a membership-context permission; `workspace.member.view` is treated as workspace governance and is not selectable as an ordinary template permission.
- 2026-06-29: Default workspace role template audit confirmed Admin still receives all asset actions through compatibility-expanded snapshots, Advanced receives every configurable asset action except governance, Basic focuses on agent/workflow creation-editing plus supporting asset reads, and Read-only keeps inspection/debug permissions without mutation.
- 2026-06-30: Fixed governance role display snapshots now ignore stale or empty member permission snapshots. Workspace owner/admin and organization owner/admin current-permissions responses are derived from the fixed role's canonical displayable asset permissions, keeping actual highest-authority checks and frontend visible/action permissions aligned without exposing retired `workspace.*`, `dashboard.*`, `prompt.*`, `content_parse.*`, or compatibility-only aggregate codes.
- 2026-06-29: Prompt/content-parse/dashboard retired-permission audit confirmed no ordinary member template or frontend permission module exposes `prompt.*`, `content_parse.*`, or `dashboard.*`. Prompt and content-parse APIs require current workspace membership via `workspace.view`; dashboard overview remains organization/workspace-context derived rather than permission-code gated.
- 2026-06-29: App-center audit confirmed runnable app lists and console overview call `useRunnableWebApps({ workspaceId: null })`, detail pages wait for authorized runnable membership before loading WebApp config, and backend organization-scope candidate lookup is separated from audience workspace matching so account/department/organization grants still work for users without joined workspaces.
- 2026-06-29: Frontend permission guardrails were hardened: selectable permissions now explicitly filter retired `workspace.*`, `prompt.*`, `content_parse.*`, and `dashboard.*` prefixes; route-access tests now fail if action matrices use legacy aggregate permissions or if non-constant frontend files consume broad `*_MANAGE_PERMISSION_CODES` groups for UI actions.
- 2026-06-29: No-workspace and organization-product copy was aligned with the current route model. Shared workspace-required states now describe only workspace asset management and scheduled tasks, and WebApp runtime workspace resolution errors no longer imply app-center audiences must join a workspace before using an authorized app.
- 2026-06-29: App-center empty/unavailable copy was aligned with organization-scope runtime authorization. The list and detail states now describe apps published to the current account instead of apps available in the current workspace, matching `useRunnableWebApps({ workspaceId: null })` and the detail `isRunnable` gate.
- 2026-06-29: Frontend route-access guardrails now also lock prompt, content-parse, scheduled-task, and file-detail direct-page contracts. Prompt pages must use workspace access and the workspace mismatch guard rather than `prompt.*`; content-parse must stay behind the workspace-scoped route without `content_parse.*`; scheduled-task mutations must use `isWorkspaceManager()`; file detail must use fine-grained file action permissions instead of legacy aggregate file codes.
- 2026-06-29: Workspace settings access decision recorded: the route can remain reachable to joined members for basic info/self-leave, but all governance mutations stay owner/admin gated and no ordinary `workspace.*` permission is reintroduced.
- 2026-06-29: Workspace overview permission copy now presents membership and governance authority as role state instead of `workspace.view`/`workspace.manage`-style ordinary permissions. Static route-access coverage locks the page away from retired workspace view/manage labels while keeping asset-area availability driven by the visible permission groups.
- 2026-06-29: Frontend source-level guardrails now fail when non-constant source files consume legacy aggregate permission literals or gate ordinary UI with retired `workspace.*`, `prompt.*`, `content_parse.*`, or `dashboard.*` permissions.
- 2026-06-30: Route-access guardrails now also scan backend non-test business code under `api/internal` and fail if compatibility-only aggregate workspace asset permissions such as `agent.manage`, `database.manage`, `database.data_edit`, `file.upload_create`, or their Go constants are consumed outside the workspace permission model compatibility source. Migrations and tests remain allowed to reference old codes for data conversion coverage.
- 2026-06-29: Workspace permission-filter service was aligned with the main `CheckWorkspacePermission` contract: organization owner/admin can see all normal organization workspaces for create-capability filters even without workspace membership, while ordinary members still require matching workspace membership and permission snapshots.
- 2026-06-29: Shared workspace management `CheckPermission` now also honors organization owner/admin global workspace authority before falling back to workspace membership snapshots.
- 2026-06-29: Legacy admin/owner middleware copy and logs now describe the shared workspace-management permission contract instead of implying authorization only comes from workspace member snapshots.
- 2026-06-29: File UI stopped directly consuming legacy `file.upload_create`, `file.move_create`, and broad file-manage groups. Upload, folder management, preview, download, update, delete, and processing actions now use exact action groups.
- 2026-06-29: Added direct-page gates for database operation logs, BI search, table records, table schema, AI table creation, table data import, table prompt editing, dataset documents, graph, hit-testing, batch-testing, and file detail. File detail backend now uses readable file permissions instead of download-only authorization.
- 2026-06-29: Agent/workflow UI and backend helpers branch action permissions by runtime type.
- 2026-06-29: Dashboard, database, knowledge base, file, and app-center helpers were aligned so create-only or published-access users do not lose their entry points.

## Verification Checklist

- Frontend type check.
- Targeted ESLint for changed frontend files.
- Backend targeted tests for agents, dataset, datasource, file, dashboard, and workspace permission model.
- Manual/browser smoke is partially covered. Covered evidence now includes advanced/basic/readonly members, a custom legacy template, restricted no-knowledge/no-database/no-file members, no-workspace app-center users, organization owner/workspace owner current-permissions, workspace admin API current-permissions, and owner/workspace-admin browser UI action passes. Final direct-page guardrail review passed for the current matrix scope; any future direct-page changes should extend `web/scripts/test-route-access.mjs` before merging.

## Verification Log

- 2026-06-30: Organization-admin no-membership workspace context was locked into the frontend guardrail. The switcher hook must merge `getManagedWorkspaces` results for organization owner/admin users, keep the organization role in the query key, and still fail closed for ordinary organization-mode permission helpers. API smoke for `codex-org-admin@example.com` returned `organization_role=admin`, `workspace_role=admin`, 84 displayable asset permissions, and zero retired or compatibility-only aggregate codes; Docker browser smoke confirmed the same account can select a managed workspace from a no-current-workspace state and then open members, agents, knowledge base, database, files, and scheduled tasks with manager actions.
- 2026-06-30: `pnpm test:route-access` passed as the final direct-page guardrail review after the owner/admin browser smoke. The guardrail script currently asserts route/workspace scope plus source-level checks for files, dataset documents/graph/hit-testing/batch-testing/settings, database search/record/import/table/table-prompt/operation-log surfaces, agent API/logs/batch-test/runtime pages, workflow run/debug/publish/edit mutations, scheduled-task workspace-manager gates, prompt/content-parse membership gates, dashboard model/parser/channel organization-admin gates, and retired/compatibility permission-code usage in frontend and backend business code.
- 2026-06-30: Local Docker web was rebuilt and restarted with `docker compose --env-file .env -f docker-compose.yaml -f compose.zgi-c.local.yaml up -d --build web`; API/web health checks passed on `http://127.0.0.1:2870/ping` and `http://127.0.0.1:2880/api/health`. Browser smoke on `http://localhost:2880` covered `codex-regression@example.com` as organization/workspace owner and `codex-workspace-admin@example.com` as workspace admin across `/console`, `/console/workspace/members`, `/console/agents`, `/console/dataset`, `/console/db`, `/console/files`, and `/console/work/task`. Both accounts saw workspace asset navigation, member add/edit-permission actions, agent import/create, knowledge-base create, database create, file folder/upload, and scheduled-task create actions where expected. The rebuilt console overview rendered workflow recent-work labels as localized `工作流` with no `dashboard.stats.*` untranslated key leakage.
- 2026-06-30: `go test ./internal/modules/workspace/service -run "TestGetWorkspaceMemberPermissions(ReturnsRawWorkspaceRole|OrganizationAdminDoesNotRequireWorkspaceMembership|CanonicalizesLegacySnapshot)|TestWorkspaceMemberAllowsPermissionRoleMatrix"` passed after fixing fixed-governance display snapshots. Local Docker `zgi-c-api-1` was rebuilt with `docker compose --env-file .env -f docker-compose.yaml -f compose.zgi-c.local.yaml up -d --build api`; `/ping` returned healthy. API smoke against `/console/api/organizations/:orgId/workspaces/:workspaceId/accounts/current/permissions` returned displayable canonical asset permissions with zero retired/compatibility codes for `codex-regression@example.com` as organization/workspace owner (85 permissions) and `codex-workspace-admin@example.com` as workspace admin with an empty stored snapshot (84 permissions).
- 2026-06-30: `pnpm test:route-access` and `go test ./internal/modules/contentparse/handler -run "TestProviderSettingsRoutes"` passed after refreshing the dashboard configuration audit. Static guardrails now lock dashboard model/parser/channel pages away from workspace member permission helpers and lock backend default-model writes, channel mutations, and content-parse provider-settings behind organization owner/admin middleware.
- 2026-06-30: `go test ./internal/modules/app/agents -run "TestAgentsService_GetRunnableWebApps|TestAgentsRepository_ListRunnableWebApps"` and `pnpm test:route-access` passed after refreshing the app-center audit. The service tests now explicitly cover a no-workspace organization member seeing an app granted to the whole organization through the dedicated `app_center` surface, while the frontend guardrail still requires organization-scope `useRunnableWebApps({ workspaceId: null })` and blocks current-workspace gates.
- 2026-06-30: `go test ./internal/modules/prompts/handler ./internal/modules/prompts/service ./internal/modules/contentparse/handler -run "TestPromptBuilderTools|TestPromptWorkspace|TestContentParse|TestPlayground|TestProviderSettings|TestContentParseRoutes|TestContentParseProviderSettings"`, `go test ./internal/modules/workspace/model ./internal/modules/workspace/service -run "TestWorkspaceMemberAllowsPermission|TestCheckMemberPermissionRejectsGovernancePermissionsFromDirectSnapshot|TestCheckPermissionAllowsOrganizationAdminWithoutWorkspaceMembership|TestOrganizationServiceCheckWorkspacePermissionWorkspaceOwnerHasAllPermissions|TestOrganizationServiceCheckWorkspacePermissionOrganizationAdminBypassesWorkspaceMembership"`, and `pnpm test:route-access` passed after re-auditing retired prompt/content-parse permissions and roleized workspace-governance authority.
- 2026-06-30: `go test ./internal/modules/system/handler ./internal/modules/system/service`, `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/utils/console-recent-work.ts src/services/types/dashboard.ts src/i18n/modules/dashboard/zh-Hans.ts src/i18n/modules/dashboard/en-US.ts`, `pnpm type-check`, and `git diff --check` passed after splitting dashboard recent-work agent/workflow scope and adding workflow recent-work routing/i18n support. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-30: `go test ./internal/modules/file_process/handler -run "TestAuthorizeWorkspaceUploadUsesDirectWorkspacePermission|TestAuthorizeWorkspaceTextCreateUsesDirectWorkspacePermission|TestAuthorizeWorkspaceUploadLegacyFallback"`, `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/files/file-management-content.tsx src/components/files/file-sidebar.tsx src/i18n/modules/files/en-US.ts src/i18n/modules/files/zh-Hans.ts`, `pnpm type-check`, and `git diff --check` passed after exposing file text creation through a dedicated `file.text.create` sidebar action and backend helper. `git diff --check` reported only the existing CRLF normalization warning for this matrix document.
- 2026-06-30: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/db/excel-import/excel-import-shell.tsx`, `pnpm type-check`, and `git diff --check` passed after gating database Excel import-error detail requests by `database.import.errors.view`. `git diff --check` reported only the existing CRLF normalization warning for this matrix document.
- 2026-06-30: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/workflow/index.tsx src/components/workflow/store/store.ts src/components/workflow/ui/workflow-run-panel/index.tsx src/components/workflow/ui/workflow-chat-panel/hooks/use-workflow-chat-panel-state.tsx`, `pnpm type-check`, and `git diff --check` passed after gating console workflow persisted run-event replay by `workflow.events.view`. `git diff --check` reported only the existing CRLF normalization warning for this matrix document.
- 2026-06-30: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs`, and `git diff --check` passed after adding static scheduled-task backend guardrails for `workspace.view` read handlers and `workspace.manage` mutation handlers. `git diff --check` reported only the existing CRLF normalization warning for this matrix document.
- 2026-06-30: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs`, and `git diff --check` passed after extending route-access guardrails to reject backend non-test business-code usage of compatibility-only aggregate workspace asset permission literals and Go constants.
- 2026-06-30: `go test ./internal/modules/workspace/handler -run "TestGetCurrentOrganizationMembers"`, `go test ./internal/modules/workspace/service -run "TestGetVisibleOrganizationMemberAccountIDsLimitsNonAdminScope|TestGetUserAllWorkspacesInOrganizationDoesNotUseOrganizationAdminBypass"`, and `git diff --check` passed after replacing the all-organization `/organizations/current/members` response for non-admin callers with department-subtree plus visible-workspace member scoping. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after deferring member-assignment and runtime-audience dialog role-template/department/workspace catalog queries behind dialog-open state.
- 2026-06-29: `go test ./internal/modules/workspace/handler -run "TestListWorkspacePermissions|TestListWorkspaceRoles|TestGetWorkspaceRole"` and `git diff --check` passed after moving workspace role-template permission catalog/list/detail reads behind fixed manager authority. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-29: `go test ./internal/modules/workspace/handler -run "TestApplyWorkspaceRoleTemplate|TestUpdateOrganizationWorkspaceMemberPermissions"` and `go test ./internal/modules/workspace/service -run "TestApplyWorkspaceRoleTemplate"` passed after moving role-template application authorization from an organization-admin handler gate to the existing per-target workspace permission checks.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/hooks/organization/use-organization-roles.ts src/app/console/workspace/members/page.tsx`, and `pnpm type-check` passed after delaying workspace member-page role-template queries behind manager authority.
- 2026-06-29: `go test ./internal/modules/workspace/handler -run "TestMembersHandler"` and `git diff --check` passed after rejecting direct `permissions` payloads on legacy workspace invite routes, adding `workspace.view` to current dataset-operator lookup, and adding `workspace.member.manage` to current default email invite. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-29: `go test ./internal/modules/workspace/handler -run "TestMembersHandler"` and `git diff --check` passed after moving legacy `/workspaces/current/members...` detail/invite/delete routes to the same pre-query workspace governance checks as the organization-scope member management APIs, and after making current role updates use the active workspace join. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-29: `go test ./internal/modules/workspace/handler -run "TestGetOrganizationWorkspaceMemberDetailByIDReturnsHasMobile|TestGetOrganizationWorkspaceMembersRequiresMemberViewPermission|TestGetOrganizationWorkspaceMemberOptionsUsesWorkspaceViewAndHidesManagementFields|TestGetOrganizationWorkspaceMemberOptionDetailUsesWorkspaceViewAndHidesManagementFields|TestGetOrganizationWorkspaceAssetsRequiresMemberManagePermission|TestOrganizationWorkspaceRoutesRejectCrossOrganizationWorkspaceBeforePermission"`, `pnpm test:route-access`, and `git diff --check` passed after adding handler regression coverage for organization-scope workspace governance/member-option boundaries. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-29: `go test ./internal/modules/workspace/handler -run "TestGetOrganizationWorkspaceMembersRequiresMemberViewPermission|TestGetOrganizationWorkspaceMemberOptionsUsesWorkspaceViewAndHidesManagementFields|TestGetOrganizationWorkspaceMemberDetailByIDReturnsHasMobile"`, `go test ./internal/modules/workspace/handler -run "TestGetOrganizationWorkspaceMembers|TestGetOrganizationWorkspaceMember"`, `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after splitting workflow approval member lookup onto workspace-view `member-options` endpoints while keeping organization-scope member management endpoints behind `workspace.member.view`.
- 2026-06-29: `go test ./internal/modules/workspace/model -run "TestBuiltinMemberPermissionsIncludeCompatibleFineCodes|TestDefaultWorkspaceMemberPermissionStringsDoNotExposeCompatibilityPermissions|TestEffectiveWorkspaceMemberPermissionStringsFallsBackForLegacyRole|TestWorkspaceMemberAllowsPermission"`, `go test ./internal/modules/workspace/service -run "TestWorkspaceMemberAllowsPermissionRoleMatrix|TestOrganizationServiceCheckWorkspacePermissionRejectsGovernanceFromDirectSnapshot|TestOrganizationServiceCheckWorkspacePermissionAllowsAdminGovernanceWithEmptySnapshot|TestOrganizationServiceCheckWorkspacePermissionWorkspaceOwnerHasAllPermissions|TestOrganizationServiceCheckWorkspacePermissionOrganizationAdminBypassesWorkspaceMembership"`, `go test ./internal/modules/workspace/handler -run "TestMembersHandler|TestGetWorkspaceMembers"`, `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after moving `workspace.member.view` under workspace governance and gating the frontend members route/sidebar/overview links by manager authority.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, `go test ./internal/modules/dataset/handler -run TestAuthorizeDatasetUpdateAccessUsesDatasetUpdatePermission`, and `git diff --check` passed after aligning dataset settings route/sidebar/direct-page metadata access with `knowledge_base.update`. `git diff --check` reported only the existing CRLF normalization warning for this matrix document.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, `go test ./internal/modules/app/workflowtest`, and `git diff --check` passed after aligning agent API/logs/batch-test direct-page metadata gates and batch-test root/sidebar visibility with the backend `workflow.runtime_access.manage`, `*.logs.view`, `workflow.view`, `workflow.update`, and `workflow.debug` action split. `git diff --check` reported only the existing CRLF normalization warning for this matrix document.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after gating AGENT/workflow direct editor pages and their metadata/config requests behind editor-related permissions. `git diff --check` reported only the existing CRLF normalization warning for this matrix document.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs`, and `git diff --check` passed after adding static guardrails for database BI search, operation logs, table records, table structure, AI table creation, table import, and table prompt direct-page gates.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs`, and `git diff --check` passed after adding static guardrails for the workflow-only API key/docs route and runtime-type-specific agent/workflow log query gates.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/workflow/index.tsx`, `pnpm type-check`, and `git diff --check` passed after requiring `workflow.update` as the frontend dependency for publishing the current workflow draft.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after gating workflow lifecycle bootstrap/default prompt normalization/default model injection behind resolved `workflow.update` permission.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after propagating workflow read-only state into container drop overlays.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after gating workflow source-handle, empty-container, stale modal, and delayed create-node actions by `workflow.update` edit authority.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after gating workflow manual resize handles and note-node inline editing by `workflow.update` edit authority.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after adding `workflow.update` read-only guards to shared workflow node-data and graph-operation helpers.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after adding `workflow.update` read-only guards to workflow automatic node dimension sync.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after adding local `workflow.update` read-only guards to workflow editor shell actions and approval action-handle edge sync.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/app/console/dataset/[datasetId]/page.tsx`, `pnpm type-check`, and `git diff --check` passed after making the dataset detail root route choose the first permission-compatible child page.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/datasets/dataset-card.tsx`, and `pnpm type-check` passed after routing dataset cards through the permission-aware detail root.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/hooks/dataset/use-datasets.ts src/components/datasets/hit-testing/components/result-item.tsx`, and `pnpm type-check` passed after routing dataset creation through the detail root and gating hit-testing document links by document-view permission.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/utils/agent-detail-routes.ts src/app/console/agents/[agentId]/page.tsx src/components/agents/agent-sidebar.tsx`, `pnpm type-check`, and `git diff --check` passed after making the agent/workflow detail root and sidebar choose permission-compatible child pages.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/app/console/agents/[agentId]/page.tsx src/components/agents/agent-sidebar.tsx src/components/agents/agent-dialog/create-dialog.tsx src/components/agents/templates/use-create-from-template.ts`, `pnpm type-check`, and `git diff --check` passed after preserving agent/workflow detail reachability for create/import/update-only custom templates and routing create/template success through the detail root.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/app/console/agents/[agentId]/page.tsx src/components/prompts/prompt-usage-summary.tsx`, and `pnpm type-check` passed after routing prompt usage references through the permission-aware agent detail root while preserving `nodeId` query parameters.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/agents/agent-card.tsx`, and `pnpm type-check` passed after routing agent/workflow list cards through the permission-aware detail root.
- 2026-06-29: Local restricted-account browser smoke found that the Docker web build hid `知识库`/`数据库`/`文件管理` navigation for a member with only agent/workflow permissions, and direct `/console/dataset` plus `/console/db` showed `访问受限`, but direct `/console/files` still rendered the file table. Source fix added the missing file-list route guard; Docker page re-smoke requires rebuilding the web image.
- 2026-06-29: Local API smoke for `codex-restricted@example.com` via `/console/api/login` and `/console/api/organizations/:orgId/workspaces/:workspaceId/accounts/current/permissions` returned 9 agent/workflow permissions with zero `file.*`, `knowledge_base.*`, or `database.*` permissions, matching the intended restricted template setup.
- 2026-06-29: Local Docker browser re-smoke passed for the same restricted account after rebuilding `zgi-web:branch-c` and recreating `zgi-c-web-1`: direct `/console/files`, `/console/dataset`, and `/console/db` render the shared access-denied state with no asset table/create UI; sidebar asset entries for files, knowledge base, and database remain hidden; direct `/console/agents` renders `智能体管理`.
- 2026-06-29: Local Docker API was rebuilt and restarted before management-authority validation. `/console/api/organizations/:orgId/workspaces/:workspaceId/accounts/current/permissions` was rechecked for four local accounts: organization owner `2945414535@qq.com` and workspace owner `test2@zgi.com` both return owner-sourced full asset permissions across agent, workflow, knowledge base, database, and file; ordinary role-template member `asset-move-member@example.com` returns 37 displayable fine-grained permissions with no `database.ai_query` or `file.upload_create` compatibility-only grants; restricted direct member `codex-restricted@example.com` returns only 9 agent/workflow runtime permissions and no knowledge-base/database/file readable permissions. None of the four responses reintroduced `workspace.*`, `dashboard.*`, `prompt.*`, or `content_parse.*` ordinary permission codes.
- 2026-06-29: Local Docker role-template UI smoke covered Advanced, Basic, and Read-only member templates. Disposable local accounts `codex-advanced@example.com` and `codex-readonly@example.com` were added to the test workspace with the default Advanced/Read-only role templates, while `asset-move-member@example.com` covered Basic. The current-permissions API returned 85/37/24 displayable permissions respectively, with no retired ordinary permission prefixes or compatibility-only aggregate grants. Browser smoke on `http://localhost:2880` confirmed all three templates can open readable asset surfaces; Advanced shows agent create, knowledge-base create, database create, folder/file create, and upload actions; Basic shows agent create and file upload but not knowledge-base/database creation; Read-only shows readable agent/knowledge-base/database/file pages without agent create, knowledge-base create, database create, upload, or new-folder buttons.
- 2026-06-29: Legacy custom-template compatibility smoke found and fixed one backend display inconsistency. A disposable local role template with raw legacy permissions (`agent.manage`, `knowledge_base.manage`, `database.ai_query`, `database.data_edit`, `file.upload_create`, `file.move_create`) plus retired prefixes (`workspace.*`, `dashboard.*`, `prompt.*`, `content_parse.*`) was assigned to `codex-legacy-template@example.com`. Role list/detail APIs already returned a clean 45-permission fine-grained snapshot, but workspace member permission detail still returned `agent.manage` and `knowledge_base.manage`. `GetWorkspaceMemberPermissions` now returns `workspaceMemberDisplayPermissionStrings` instead of the authorization-oriented effective snapshot, so self and owner lookup responses both return the clean 45-permission display snapshot. Browser smoke confirmed the legacy-template account can open agent, knowledge-base, database, and file routes; expanded create/upload actions appear where the legacy grants imply them, and no access-denied state or deleted-template blank selector state appears.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/app/console/files/page.tsx`, and `pnpm type-check` passed after adding the `/console/files` visible-permission guard and static assertion.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after aligning agent runtime knowledge binding UI with backend readable knowledge permission semantics.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after gating workflow knowledge-retrieval candidate loading by knowledge readable permissions and making recall settings honor workflow read-only state.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after aligning agent runtime database binding UI with the backend requirement for both `database.ai_query.read` and `database.record.view`.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, `pnpm type-check`, and `git diff --check` passed after aligning workflow call-database and SQL-generator database metadata UI with the shared database read-binding permission pair.
- 2026-06-29: `go test ./internal/modules/contentparse/...`, `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs`, `pnpm type-check`, and `git diff --check` passed after moving content-parse provider-settings behind organization owner/admin middleware and locking dashboard model/parser/channel configuration to organization-admin capabilities instead of workspace member permissions. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-29: `go test ./internal/modules/datasource/handler`, `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/constants/permissions.ts src/app/console/db/[dbId]/layout.tsx src/app/console/db/[dbId]/page.tsx` passed after aligning database table metadata read permissions.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/db/table-data/index.tsx src/app/console/db/[dbId]/table/[tableId]/data/page.tsx`, `pnpm type-check`, and `git diff --check` passed after splitting database current-table import UI into `database.import.execute` for direct Excel import and `database.import.analyze` plus `database.record.create` for AI-assisted smart ingest. `git diff --check` reported only CRLF normalization warnings.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/app/console/db/page.tsx` passed after splitting database list create and edit frontend gates.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/app/console/db/page.tsx src/app/console/db/[dbId]/layout.tsx` passed after constantizing database create and operation-log frontend gates.
- 2026-06-29: `pnpm test:route-access` and `pnpm exec eslint scripts/test-route-access.mjs` passed after adding static guardrails for the database Excel import direct page. `/console/db/:dbId/import-excel` delegates to `ExcelImportShell`, and the shell blocks direct access unless `database.import.analyze` or `database.import.execute` is present.
- 2026-06-29: `go test ./internal/modules/file_process/handler`, `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/components/files/file-list.tsx src/components/files/detail/file-detail-shell.tsx` passed after separating file readable and browse permissions.
- 2026-06-29: `pnpm test:route-access`, `pnpm exec eslint scripts/test-route-access.mjs src/components/files/related-resources-popover.tsx`, and `pnpm type-check` passed after routing file related-resource dataset links through the permission-aware dataset detail root.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/components/datasets/dataset-card.tsx` passed after splitting dataset folder move and workspace move UI permissions.
- 2026-06-29: `pnpm test:route-access` and `pnpm exec eslint scripts/test-route-access.mjs src/components/agents/agent-card.tsx` passed after narrowing card-level YAML export to workflow runtimes.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/app/console/agents/page.tsx src/components/agents/agent-dialog/create-dialog.tsx src/components/agents/templates/template-gallery-dialog.tsx` passed after aligning agent/workflow blank-create and workflow import/template frontend gates.
- 2026-06-29: `go test ./internal/modules/workspace/service -run "TestWorkspaceAssetMove"` passed after making asset move permission checks branch by resolved agent/workflow runtime type.
- 2026-06-29: `pnpm type-check` passed.
- 2026-06-29: Targeted ESLint passed for changed agent/workflow/file permission UI files; reran for `agent-runtime/header.tsx`, `agent-runtime/use-agent-runtime-page-model.tsx`, `agent-runtime` prompt/orchestration/section read-only files, and `agent-detail-routes.ts` after runtime-access/header/read-only alignment.
- 2026-06-29: `pnpm test:route-access` passed after preserving the workflow-only API-key/docs detail route contract and adding agent runtime publish-dropdown/runtime-access plus runtime-config read-only assertions.
- 2026-06-29: `go test ./middleware` passed after aligning legacy middleware comments/logging with the shared workspace-management permission contract.
- 2026-06-29: `go test ./internal/modules/workspace/service -run "TestCheckPermission"` and `go test ./internal/modules/workspace/service -run "TestCheckMemberPermission"` passed after aligning the shared workspace-management permission helper with organization owner/admin authority.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and targeted ESLint passed after excluding compatibility-only aggregate permissions from selectable role/member permission lists while expanding them during save normalization.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and targeted ESLint passed after removing compatibility-only aggregate permissions from frontend database/file action groups and direct-page checks.
- 2026-06-29: `pnpm test:route-access`, targeted ESLint, and `pnpm type-check` passed after splitting workflow batch-test UI route/button guards across `workflow.view`, `workflow.logs.view`, `workflow.update`, `workflow.debug`, and `workflow.run.stop`.
- 2026-06-29: `go test ./internal/modules/app/workflowtest` passed after the workflow batch-test frontend alignment, confirming the backend permission assertions for settings/cases, batch/result reads, debug execution, and stop actions remain green.
- 2026-06-29: `go test ./internal/modules/workspace/model -run "TestWorkspaceMemberAllowsPermission|TestDefaultWorkspaceMemberPermissionStringsDoNotExposeCompatibilityPermissions|TestCanonicalAssignableWorkspacePermissionSnapshotStringsExpandsCompatibilityPermissionsWithoutRetainingThem"` passed.
- 2026-06-29: `go test ./internal/modules/workspace/service -run "TestWorkspaceMemberAllowsPermissionRoleMatrix|TestOrganizationServiceCheckWorkspacePermissionAllowsAdminGovernanceWithEmptySnapshot|TestOrganizationServiceCheckWorkspacePermissionWorkspaceOwnerHasAllPermissions|TestOrganizationServiceCheckWorkspacePermissionOrganizationAdminBypassesWorkspaceMembership"` passed.
- 2026-06-30: Workspace owner-transfer API smoke covered the local `go-sqlite3` gap. `go test ./internal/modules/workspace/service -run "TestTransferOwner"` remains blocked in this local environment because `CGO_ENABLED=0` makes `go-sqlite3` a stub, but Docker/Postgres verification created a disposable workspace with two historical `owner` rows, called `POST /console/api/organizations/:organization_id/workspaces/:workspace_id/transfer-ownership` as `codex-org-admin@example.com` with no workspace membership, and confirmed the API succeeded, the target remained the sole owner, the old owner was downgraded to admin with the admin role template, and the organization admin was not inserted into `workspace_members`; the disposable workspace was removed afterward.
- 2026-06-30: `pnpm test:route-access` now includes source-level owner-transfer guardrails, confirming `TransferOwner` still allows organization owner/admin authority without workspace membership, scans all workspace joins, demotes every non-target owner to the builtin admin template, and marks the target as the owner permission source. This keeps the Docker/Postgres smoke scenario covered by a repeatable check while the local SQLite-backed Go test remains unavailable.
- 2026-06-29: `pnpm test:route-access` passed after the default-template and retired-permission static audit, confirming the frontend route/action contract still holds.
- 2026-06-29: `go test ./internal/modules/workspace/model -run "TestExpandWorkspacePermissionCodesForCompatibility|TestBuiltinMemberPermissionsIncludeCompatibleFineCodes|TestDefaultWorkspaceMemberPermissionStringsDoNotExposeCompatibilityPermissions|TestWorkspaceMemberAllowsPermission"` passed.
- 2026-06-29: `go test ./internal/modules/prompts/... ./internal/modules/contentparse/...` passed, confirming prompt/content-parse workspace membership gates remain green without reintroducing retired member permissions.
- 2026-06-29: `go test ./internal/modules/app/agents -run "Test.*Runnable|Test.*RuntimeSurfaces|TestAgentRuntimeAuthorizationFromUpdateRequest"`, `go test ./internal/modules/app/runtimeauth -run "Test.*Surface|Test.*Authorized|Test.*Grant"`, and `go test ./internal/modules/user/auth/service -run "TestGetAccountCapabilities"` passed for app-center/runtime surface authorization.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/constants/permissions.ts` passed after hardening frontend permission guardrail assertions.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/i18n/modules/common/zh-Hans.ts src/i18n/modules/common/en-US.ts src/i18n/modules/webapp/zh-Hans.ts src/i18n/modules/webapp/en-US.ts` passed after adding stale workspace-only product copy guardrails.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/i18n/modules/webapp/zh-Hans.ts src/i18n/modules/webapp/en-US.ts` passed after adding app-center current-workspace copy guardrails.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, and `pnpm exec eslint scripts/test-route-access.mjs src/app/console/prompts/page.tsx src/app/console/prompts/[promptId]/page.tsx src/app/console/developer/content-parse/page.tsx src/components/content-parse/playground/content-parse-playground.tsx src/app/console/work/task/page.tsx src/components/automation/task-workbench.tsx src/app/console/files/[fileId]/page.tsx src/components/files/detail/file-detail-shell.tsx` passed after adding direct-page guard assertions for retired prompt/content-parse permissions, scheduled-task manager authority, and file fine-grained actions.
- 2026-06-29: `pnpm test:route-access` passed after adding a source-level guard that blocks legacy aggregate permission literals and retired permission checks outside the compatibility constants.
- 2026-06-29: `go test ./internal/modules/workspace/service -run "TestWorkspacePermissionFilter"` passed after aligning workspace permission filtering with organization owner/admin global workspace authority.
- 2026-06-29: `go test ./internal/modules/workspace/model ./internal/modules/file_process/handler ./internal/modules/system/handler` passed after backend canonical snapshot and file upload permission alignment.
- 2026-06-29: `go test ./internal/modules/datalibrary/handler` passed after replacing data-library `knowledge_base.manage` checks with document/index action permissions.
- 2026-06-29: `go test ./internal/modules/workspace/model -run "Test(DefaultWorkspaceMemberPermissionStringsDoNotExposeCompatibilityPermissions|CanonicalAssignableWorkspacePermissionSnapshotStringsExpandsCompatibilityPermissionsWithoutRetainingThem|CanonicalWorkspacePermissionSnapshotStringsReplacesDeprecatedAssetCoarseCodes|EffectiveWorkspaceMemberPermissionStringsExpandsLegacyCoarseGrant)$"` passed.
- 2026-06-29: `go test ./internal/modules/workspace/service -run "TestBuiltinWorkspaceRoleSummaryPermissionsCanonicalizesCompatibilityCodes|TestApplyWorkspaceMemberDirectPermissionSnapshotExpandsPermissions|TestGetWorkspaceMemberPermissionsReturnsRawWorkspaceRole|TestApplyWorkspaceRoleTemplate"` passed. Full `go test ./internal/modules/workspace/service` is blocked in this local environment by `go-sqlite3` requiring CGO while the binary is built with `CGO_ENABLED=0`.
- 2026-06-29: Backend targeted tests passed for `api_key`, `app/agents`, `app/workflow`, `dataset/handler`, `file_process/handler`, `datasource/handler`, `system/handler`, `workspace/model`, `prompts`, `contentparse`, and `automation/handler`.
- 2026-06-29: `go test ./internal/modules/app/agents` passed after moving agent runtime surface/status authorization to `agent.runtime_access.manage`.
- 2026-06-29: Local Docker browser smoke unblocked with disposable local test credentials. Ordinary role-template member `asset-move-member@example.com` can log in at `http://localhost:2880/login`, open `/console`, see the overview panel, workspace-scoped asset navigation, recent work, and organization-scoped app-center shortcuts.
- 2026-06-29: Local Docker no-workspace browser/API smoke passed after adding a temporary account app-center grant for `codex-no-workspace@example.com`. The account has no current workspace, `/console` shows `0 个工作空间`, hides workspace asset navigation (`agents`, `dataset`, `files`, `db`), shows the account-granted app-center shortcut, and opens `/console/work/app/2bbf0f19-8f39-4363-b1a5-9fc528f4be60` without a no-access state.
- 2026-06-29: `pnpm test:route-access`, `pnpm type-check`, `go test ./internal/modules/workspace/service -run "TestWorkspacePermissionFilter|TestCheckPermission|TestCheckMemberPermission"`, `go test ./internal/modules/app/agents -run "Test.*Runnable|Test.*RuntimeSurfaces|TestAgentRuntimeAuthorizationFromUpdateRequest"`, and `go test ./internal/modules/app/runtimeauth -run "Test.*Surface|Test.*Authorized|Test.*Grant"` passed after the browser smoke pass.
- 2026-06-29: `go test ./internal/modules/workspace/service -run "TestWorkspaceMemberResponsePermissionsExcludeRetiredAndCompatibilityCodes|TestExpandWorkspaceMemberStoredPermissionsReturnsDisplayablePermissions|TestCheckPermission|TestCheckMemberPermission|TestUpdateMemberDirectPermissions|TestApplyWorkspaceMember"` and `go test ./internal/modules/workspace/model -run "TestDefaultWorkspaceMemberPermissionStringsDoNotExposeCompatibilityPermissions|TestCanonicalAssignableWorkspacePermissionSnapshotStringsExcludesDashboardPermissions|TestEffectiveWorkspaceMemberPermissionStringsExcludesDashboardPermissions|TestCanonicalWorkspacePermissionSnapshotStringsReplacesDeprecatedAssetCoarseCodes|TestAllWorkspacePermissionCodes"` passed after canonicalizing member permission response payloads. A broader workspace-service run that includes SQLite-backed tests remains blocked locally by `CGO_ENABLED=0`/`go-sqlite3`.
- 2026-06-29: `go test ./internal/migrations -run "TestCanonicalWorkspaceAssignablePermissionJSON|TestBuildWorkspaceMemberPermissionSeedCanonicalizesLegacyCoarsePermissions|TestDefaultWorkspaceRoleTemplateDefinitionsExcludeNonConfigurablePermissions"`, `go test ./internal/modules/workspace/model -run "TestDefaultWorkspaceMemberPermissionStringsDoNotExposeCompatibilityPermissions|TestCanonicalAssignableWorkspacePermissionSnapshotStringsExcludesDashboardPermissions|TestCanonicalAssignableWorkspacePermissionSnapshotStringsExpandsCompatibilityPermissionsWithoutRetainingThem|TestCanonicalWorkspacePermissionSnapshotStringsReplacesDeprecatedAssetCoarseCodes|TestEffectiveWorkspaceMemberPermissionStringsExcludesDashboardPermissions|TestAllWorkspacePermissionCodes"`, `go test ./internal/modules/workspace/service -run "TestWorkspaceMemberResponsePermissionsExcludeRetiredAndCompatibilityCodes|TestExpandWorkspaceMemberStoredPermissionsReturnsDisplayablePermissions|TestCheckPermission|TestCheckMemberPermission|TestUpdateMemberDirectPermissions|TestApplyWorkspaceMember"`, and `pnpm test:route-access` passed after adding the canonical permission snapshot data-fix migration.
- 2026-06-29: `go test ./internal/modules/app/agents`, `go test ./internal/modules/workspace/service -run "TestWorkspaceAssetMove"`, and `pnpm test:route-access` passed after aligning agent/workflow create and legacy tenant-move backend permission checks with the action matrix.
- 2026-06-30: Local Docker browser smoke rechecked disposable permission accounts on `http://localhost:2880` after the latest workspace-permission changes. `codex-restricted@example.com` keeps only agent/workflow-visible asset navigation, can open `/console/agents`, and direct `/console/dataset`, `/console/db`, and `/console/files` render the shared access-limited state without create/upload actions. `asset-move-member@example.com` (Basic) opens overview, agents, knowledge base, database, files, and app center; it shows agent create and file upload actions, while knowledge-base and database create actions stay hidden. `codex-readonly@example.com` opens readable asset pages but shows no agent create, knowledge-base create, database create, upload, or new-folder actions. `codex-no-workspace@example.com` shows `0` workspaces, hides workspace asset navigation, and still opens the organization-scoped app center with granted apps.
- 2026-06-30: `pnpm test:route-access`, targeted ESLint for the workspace role-template helper and affected member-management pages, and `pnpm type-check` passed after centralizing selectable workspace role-template classification and adding static guardrails for add-member, member-permission, organization permission-template, and organization workspace-detail entry points.
