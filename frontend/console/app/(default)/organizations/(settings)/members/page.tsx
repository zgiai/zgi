import MembersPage from "./membersPage"

export const metadata = {
    title: "Members",
    description: "Members",
}

export default function Members() {
    return (
        <MembersPage />
    )
}