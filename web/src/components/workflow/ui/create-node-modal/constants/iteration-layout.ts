// Centralized iteration layout constants to avoid drift with adjustIterationLayout
export const ITER_HEADER_H = 32;
export const PAD_X = 12;
export const PAD_Y = 12;
export const GAP_X = 50;

// Offsets used by iteration content area (padding + header)
export const ITER_CANVAS_OFFSET_X = PAD_X;
export const ITER_CANVAS_OFFSET_Y = PAD_Y + ITER_HEADER_H;
